<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_composer"
    style="@style/Widget.GeoAgent.Composer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/spacing_md"
    android:layout_marginBottom="@dimen/spacing_md">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:paddingTop="@dimen/spacing_md"
        android:paddingBottom="@dimen/spacing_sm">

        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/spacing_sm"
            android:background="@drawable/bg_status_pill"
            android:paddingHorizontal="@dimen/spacing_md"
            android:paddingVertical="@dimen/spacing_xs"
            android:textColor="@color/primary"
            android:textSize="12sp"
            android:visibility="gone" />

        <EditText
            android:id="@+id/et_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:hint="@string/composer_hint"
            android:importantForAutofill="no"
            android:inputType="textMultiLine|textCapSentences"
            android:maxLines="5"
            android:minHeight="@dimen/composer_input_min_height"
            android:paddingVertical="@dimen/spacing_xs"
            android:textColor="@color/on_background"
            android:textColorHint="@color/text_muted"
            android:textSize="15sp" />

        <!-- RAG settings (hidden by default) -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_rag_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:visibility="gone"
            app:cardBackgroundColor="@color/chip_bg"
            app:cardCornerRadius="@dimen/radius_md"
            app:cardElevation="0dp"
            app:strokeColor="@color/outline_subtle"
            app:strokeWidth="0.5dp">

            <LinearLayout
                android:id="@+id/layout_rag_settings"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/spacing_md">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="文档来源数量"
                    android:textAppearance="@style/TextAppearance.GeoAgent.LabelSmall" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/slider_top_k"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:stepSize="1"
                    android:valueFrom="1"
                    android:valueTo="10"
                    app:thumbColor="@color/primary"
                    app:trackColorActive="@color/primary"
                    app:trackColorInactive="@color/outline" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_xs"
                    android:text="最小相关度"
                    android:textAppearance="@style/TextAppearance.GeoAgent.LabelSmall" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/slider_min_relevance"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:stepSize="0.01"
                    android:valueFrom="0"
                    android:valueTo="1"
                    app:thumbColor="@color/primary"
                    app:trackColorActive="@color/primary"
                    app:trackColorInactive="@color/outline" />
            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

        <!-- Bottom row: chips + send button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <HorizontalScrollView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:scrollbars="none">

                <com.google.android.material.chip.ChipGroup
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:chipSpacingHorizontal="@dimen/spacing_sm"
                    app:singleLine="true">

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chip_web_search"
                        style="@style/Widget.GeoAgent.Chip.Assist"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:checkable="true"
                        android:text="@string/smart_search"
                        app:chipIcon="@drawable/ic_globe"
                        app:chipIconVisible="true" />

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chip_image"
                        style="@style/Widget.GeoAgent.Chip.Assist"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="图片" />

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chip_rag_settings"
                        style="@style/Widget.GeoAgent.Chip.Assist"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="检索设置"
                        android:visibility="gone" />
                </com.google.android.material.chip.ChipGroup>
            </HorizontalScrollView>

            <!-- Circle send button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/btn_send"
                android:layout_width="@dimen/send_btn_size"
                android:layout_height="@dimen/send_btn_size"
                android:layout_marginStart="@dimen/spacing_sm"
                android:contentDescription="@string/send"
                android:enabled="false"
                android:src="@drawable/ic_send_arrow"
                app:backgroundTint="@color/deepseek_send_disabled"
                app:fabCustomSize="@dimen/send_btn_size"
                app:tint="@color/white" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
