<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GeoScientist" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary palette -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>

        <!-- Secondary palette -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_container</item>

        <!-- Surface / Background -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/on_background</item>

        <!-- Outline -->
        <item name="colorOutline">@color/outline</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@color/status_bar_bg</item>
        <item name="android:navigationBarColor">@color/nav_bar_bg</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowSplashScreenBackground">@color/background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_blank</item>

        <!-- Cards -->
        <item name="materialCardViewStyle">@style/Widget.GeoAgent.Card</item>
        <item name="materialCardViewOutlinedStyle">@style/Widget.GeoAgent.Card.Outlined</item>

        <!-- Text appearances -->
        <item name="textAppearanceBodyLarge">@style/TextAppearance.GeoAgent.BodyLarge</item>
        <item name="textAppearanceBodyMedium">@style/TextAppearance.GeoAgent.BodyMedium</item>
        <item name="textAppearanceLabelSmall">@style/TextAppearance.GeoAgent.LabelSmall</item>
    </style>
</resources>
